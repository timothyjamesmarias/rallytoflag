<script setup>
import { useForm } from '@inertiajs/inertia-vue3';
import Input from '@/Components/Input.vue';
import PrimaryButton from '@/Components/PrimaryButton.vue';
import { ref } from 'vue';

const form = useForm({
  query: '',
});

const search = () => {
  form.get(route('event.index'), {
    preserveState: true,
    preserveScroll: true,
  });
}
</script>

<template>
  <Head title="Welcome" />
<div class="h-96 bg-green-100">
</div>
</template>
